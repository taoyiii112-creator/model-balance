## 日期：2026-08-09

完成：修复 Tcl 中文路径 bug——Codex 自带 Python 的 Tcl 在中文绝对路径（C:\Users\小张\...）下找不到 init.tcl，Tk 窗口创建失败（此前验证方法误判，实际 exe 一直打不开；dist\data\logs\crash.log 为证）。run.py 启动时自动把 Tcl/Tk 数据复制到纯 ASCII 路径（D:\codexProject\mb_tcl_stage）并设置 TCL_LIBRARY/TK_LIBRARY；源码版与 exe 版均实测通过（日志"Tk 窗口已创建"+ 自动采集正常，无新 crash）。42 项测试全过。

影响：桌面端（源码版和 exe 版）真正可打开窗口，自动采集、局域网同步、图表全部可用。

备注：D:\codexProject\mb_tcl_stage 为 Tcl/Tk ASCII 暂存目录（打包与运行共用，勿删）；dist\data 旧崩溃日志已清理。
## 日期：2026-08-08

完成：手机端同步接口统一 14 天口径——`lan-sync /api/codex-usage` 由返回全部历史改为只返回最近 14 天记录（新增 `codex_usage.filter_recent`，响应增加 `keep_days: 14` 字段），与桌面端"数据库只保留 14 天"一致。实测 3295 条记录全部在 14 天内；42 项测试全过（新增过滤用例 + 接口字段断言）。

影响：手机端与桌面端看到的数据范围一致，不再出现手机上翻到 14 天前记录的情况。

备注：exe 需重新打包后生效。
## 日期：2026-08-08

完成：局域网同步信息显示优化——应用内从显示一整行 `http://IP:8002 令牌: xxx` 改为清晰显示 `IP: <ip>:8002 | 令牌: 前8位…`，并新增三个复制按钮：复制地址（完整 http://IP:8002，给手机端填）、复制IP、复制令牌（完整值），复制后状态栏提示。41 项测试全过。

影响：不再混淆"HTTP 地址"与"IP"；手机端填写与调试更省事，一键复制。

备注：exe 需重新打包后生效。
## 日期：2026-08-08

完成：桌面端自动采集 Codex 用量——App 启动即同步一次、之后每 30 秒后台增量扫描 ~/.codex 会话写入本地用量库（新增 `codex_usage.sync_codex_usage_to_db()`，批量写入 + 按 note 去重，只保留 14 天，清理后旧记录不会反复回填）；CLI `codex-usage --save` 改用同一函数；应用内新增"用量采集"状态显示，采集到新数据自动刷新用量区。41 项测试全过（新增批量入库与去重用例）。

影响：Token 用量记录不再停留在上次手动采集的时间，打开 App 即自动保持最新；数据库始终只保留 14 天。

备注：exe 需重新打包后生效。
## 日期：2026-08-08

完成：局域网同步应用内一键开关——桌面应用顶部勾选"局域网同步"即启动（0.0.0.0:8002），自动显示局域网 IP（get_lan_ip）与同步令牌，无需命令行；退出应用自动关闭；exe 已重新打包。全量测试通过。

影响：手机端同步从"敲命令行"变成"勾一下"。

备注：下个版本发布时随 exe 生效（需授权）。
## 日期：2026-08-08

完成：修复 codex 记录时间显示——codex-usage --save 保存时把时间转成本地时区（之前直接存 UTC，桌面端显示差 8 小时）；重建 codex 用量记录（3071 条，21 会话）。全量测试通过。

影响：桌面端"最新用量"与记录时间与本地一致。

备注：——
## 日期：2026-08-08

完成：桌面端 v0.2.3 发布 GitHub Release（经 gh-release-publish 技能，zip + exe 双资产）。包含 v0.2.2 之后的全部改动：版本显示、阈值可配置、实时用量接口、局域网同步（lan-sync + 专用令牌）、token 数据修正（14 天保留/图表排除 codex/全屏布局）、代理改可选。

影响：桌面端最新版本正式发布，含局域网同步（手机端可拉取 Codex 用量）。

备注：——
## 日期：2026-08-08

完成：README 补充局域网同步（lan-sync）使用说明；lan-sync 令牌比较改用 hmac.compare_digest（防时序攻击）。

影响：文档与功能同步；鉴权更稳妥。

备注：——
## 日期：2026-08-08

完成：局域网同步鉴权升级——改为专用同步令牌（secrets.token_hex 32 位，保存于 data/lan_sync_token.txt，gitignore 不提交；可删除重新生成），不再校验账户 API Key。手机端相应改为填写令牌（安全存储）。测试通过。

影响：局域网抓包最多泄露只读令牌，API Key 不再经过明文网络。

备注：——

## 日期：2026-08-08

完成：用量代理改为可选——桌面应用与网页版不再自动拉起代理，Token 用量正式来源定为 codex-usage（提取 Codex 会话）；代理保留代码与实时接口，需要时手动启动。33+4 项测试通过。

影响：减少闲置进程，明确用量数据来源；代理降级为按需工具。

备注：——
## 日期：2026-08-08

完成：局域网同步服务（桌面端）——新增 `lan-sync` 命令（默认 0.0.0.0:8002，GET /api/codex-usage 返回 Codex 用量 JSON，Authorization Bearer 校验 config.json 账户 Key，另有健康检查），并附启动脚本 `启动局域网同步.bat`；手机端已联调通过（一键拉取）。新增 2 项服务测试，全量测试通过。

影响：同一 WiFi 下手机可直接拉取最新 Codex 用量，无需手动导出导入。

备注：Windows 首次运行需允许防火墙放行 8002 端口。

## 日期：2026-08-08

完成：token 用量数据修正——codex-usage 改为按会话累计值取增量（精确到会话总量）；删除 2 条演示/测试记录；真实 Codex 用量 2847 条入库（account=codex）；数据库用量只保留 14 天（init_db 自动清理）；桌面端只显示 14 天 token 用量，图表默认排除 codex 账户（顶部"含Codex用量"开关）；修复全屏化图表被遮挡（窗口 1220x900、图表高度调小）。33 项 smoke 测试 + 4 项 codex 测试通过。

影响：桌面端 token 数据与真实用量一致；codex 海量用量不再淹没 DeepSeek 真实数据（默认隐藏，可开关）。

备注：发布 v0.2.3 已被用户撤回；当前为未发布开发态。
## 日期：2026-08-08

完成：Codex 本地会话用量提取（桌面端）——新增 `codex-usage` 命令：扫描 ~/.codex/sessions 与 archived_sessions 的 rollout JSONL，解析 token_count 事件（输入/命中缓存/未命中缓存/输出/总 Token），按 key 去重；支持汇总、`--export` 导出 JSON（供手机端导入）、`--save` 写入本地用量库（note 标记 codex）。实测真实数据：2791 条调用、20 会话、总 Token 5.7 亿。新增 3 项测试，全量测试通过。

影响：Codex（DeepSeek）真实用量不再依赖代理或手动记录，历史会话可追溯；为手机端数据共享提供数据源。

备注：手机端「导入 Codex JSON」与局域网同步为下一步。

## 日期：2026-08-08

完成：实时 Token 用量（桌面端）——用量代理新增 GET /api/v1/usage/realtime 接口（需任一账户 Key 认证，支持 minutes / account 参数，返回近 60 分钟记录）；桌面应用用量汇总显示"最新一条"（时间/模型/Token）。31 项测试通过。

影响：实时用量可从代理以 HTTP 方式拉取，供其他端使用（手机端接入由用户/其他会话负责，本会话不实现）。

备注：——
## 日期：2026-08-08

完成：版本号 0.2.2 → 0.2.3（未发布）。修正"代码有新功能但版本号未同步"的问题；当前 0.2.3 包含版本显示与低余额阈值可配置两个新功能。

影响：源码版双击显示 v0.2.3，与代码状态一致；发布 v0.2.3 需用户授权。

备注：——
## 日期：2026-08-08

完成：低余额预警阈值用户可配置——config.json 顶层 alert_threshold（默认 5.0），应用顶部栏输入框可改、修改自动写回；30 项测试通过（新增配置读写与非法值回退用例）。

影响：预警阈值不再硬编码，用户可自行调整。

备注：下一版本发布时随 v0.2.3（待授权）。
## 日期：2026-08-08

完成：桌面应用内显示当前版本号（窗口标题 + 顶部栏）；多端数据同步方案设计（docs/多端数据同步方案设计.md）——中心化后端 API + 离线缓存，密钥不上传，含 API/认证/冲突策略与 P1-P4 实施阶段。

影响：v0.2.2 实测通过后版本展示落地；多端同步进入"待用户确认方案 → P1 实施"阶段。

备注：下一步需用户确认后端部署位置、存储（SQLite/MySQL）、认证方式与同步时机。
## 日期：2026-08-07

完成：桌面端 v0.2.2 发布 GitHub Release（经 gh-release-publish 技能，含 zip 源码包 + model-balance.exe 双资产）；整理项目文档，清除过时内容（手机端旧状态、已完成的待办项等）。

影响：分发闭环（exe 免 Python）与更新链路完整可用；文档与当前状态一致。

备注：中转渠道、OpenAI 官方、pricing 配置、多端同步为后续待办。
## 日期：2026-08-07

完成：exe 打包（v0.2.2）——PyInstaller 单文件 exe（dist/model-balance.exe 约 12MB），`--paths src` + 隐藏导入解决打包找不到 modelbalance 的问题；双击无参数默认打开应用界面；冻结模式 PROJECT_ROOT 指向 exe 目录（数据/配置持久化）；exe 版更新支持下载 exe 资产并自动替换重启（cmd 辅助脚本）；logutil 修复（handler 挂 root，所有模块日志统一写入）。

影响：分发闭环完成——接收方免装 Python，双击即用；exe 与源码版更新机制并存。28 项测试全过。

备注：exe 首次使用需在 exe 同目录放 .env 与 config.json；发布 Release 时同时上传 zip 与 exe 资产（需用户授权）。
## 日期：2026-08-07

完成：余额趋势图（近 30 天折线，多账户分色）与低余额提醒（<¥5 红色提示）；余额快照默认自动保存（30 分钟限频）；更新改为"暂存 → 应用退出 → 安装 → 重启"机制（apply_staged.py），解决覆盖运行中文件失败；代理增加 /__mb_health 健康检查与端口占用校验（避免误判其他程序）；新增 data/logs/app.log 日志；补充 web / CLI / 快照 / 代理健康测试，28 项全过。

影响：核心目标"余额趋势"落地；更新与代理可靠性提升；版本升至 v0.2.2（待用户授权后发布）。

备注：exe 打包、多端数据同步仍待办。
## 日期：2026-08-07

完成：健壮性优化（v0.2.1）——流式代理改为边收边转发（保留打字机效果）；SQLite 增加 busy_timeout=5000；多账户余额查询并行化；桌面版更新源可配置（python run.py set-update-source / MB_UPDATE_SOURCE）；更新包下载后 zip 完整性校验、安装后自动清理；自动检查更新失败不再误报"已是最新"。

影响：代理模式实际使用体验与更新链路可靠性提升；23 项测试通过（新增流式代理端到端、更新源配置、zip 校验、清理用例）。

备注：桌面端 v0.2.0 已发布（含 zip 资产）；v0.2.1 待打包发布。
## 日期：2026-08-07

完成：手机端更新源可配置（设置→更新源地址，持久化到应用私有目录）与检查失败原因提示（v0.2.2，APK 已打包 49.9MB）。定位到手机端更新"不生效"根因：model_balance_app 仓库从未发布 Release，更新源一直返回 404。

影响：手机端更新在国内网络下可通过自建/镜像更新源使用；发布 v0.2.2 Release 后即可验证完整链路。

备注：待用户在 GitHub 创建 Release（tag v0.2.2 + 上传 app-release.apk + 写更新说明）。
## 日期：2026-08-07

完成：桌面版自动更新功能——启动/手动检查 GitHub Releases，弹窗显示新版本号、更新内容与更新包大小，一键下载安装并自动重启；新增打包脚本 make_release.py 与 打包更新包.bat；用户数据（.env / data / config.json）更新时保留。

影响：分发场景闭环——发布者打 zip 传 GitHub Release，接收方打开应用即可收到更新；19 项测试通过。

备注：首次发布需在 GitHub 创建 Release（tag v0.2.0）并上传 dist/model-balance-0.2.0.zip；更新检查访问 api.github.com。
## 日期：2026-08-07

完成：用量代理自动拉起——桌面应用 / 网页版启动时自动检查并后台启动代理（ensure_proxy），用户无需手动运行；应用窗口显示代理运行状态。

影响：自动采集链路"打开应用即可用"，无需额外操作；15 项测试通过。

备注：Windows 开机自启（登录时自动启动代理）可选，待用户确认。
## 日期：2026-08-07

完成：本地 API 代理自动采集 Token 用量（proxy 命令 + 启动用量代理.bat）。客户端 base_url 指向 http://127.0.0.1:8001/v1 即自动记录真实 usage（输入/输出/缓存命中，兼容 DeepSeek 与 OpenAI 风格，支持流式）；pricing 配置可选估算费用。

影响：用量数据从手动录入升级为自动采集，图表数据与真实调用一致；14 项测试通过（含代理端到端集成测试）。

备注：计价需各账户在 config.json 配置 pricing 单价；上游仍为各平台官方/渠道接口。
## 日期：2026-08-07

完成：图表 UI 优化——字号放大、配色更换（消费红 / Token 绿）、柱状图悬停提示（显示日期与数值）、界面标注用量为本地记录（非官方接口）。

影响：图表可读性提升；明确数据来源，避免与官网数据混淆。

备注：——
# 开发记录

## 日期：2026-08-07

完成：手机 App v0.2.5——首页新增余额趋势折线图（近 30 天，多账户下拉切换，展示本地快照）；设置页新增余额自动刷新间隔选项（15/30/60/120 秒，持久化到 settings.json，修改即生效）。analyze 零问题、25 个测试通过，APK v0.2.5（50.4MB）已打包。

影响：可直观看到余额变化趋势；刷新频率可按需调整省流量。

备注：——

## 日期：2026-08-07

完成：手机 App v0.2.4——启动时兜底清理上次安装残留的临时 APK（补上 v0.2.3 延迟清理可能被进程终止的漏洞）；更新源兼容简单 version.json 格式（version/url/size/notes/sha256，可绕开 GitHub API 限流）；403 限流给出明确提示；保存更新源加异常处理与 http(s) 格式校验。analyze 零问题、24 个测试通过，APK v0.2.4（50.1MB）已打包。

影响：更新链路更健壮，支持自建静态更新源。

备注：——

## 日期：2026-08-07

完成：手机 App v0.2.3——更新安装完成后延迟自动清理下载的临时 APK（等系统安装器读取完成后删除），避免 App 私有目录残留约 50MB 文件。analyze 零问题、21 个测试通过，APK v0.2.3（50.3MB，正式签名）已打包。

影响：更新链路更干净，不占额外空间。

备注：——

## 日期：2026-08-07

完成：手机 App 稳定性优化（v0.2.2）——修复应用内更新下载流程 bug（原为弹窗关闭后才开始下载，进度条失效）；下载 APK 后 SHA256 完整性校验（支持 Release 附带 .sha256 资产）；正式签名 keystore（替换 debug 签名，key.properties 已 gitignore）；余额合计按币种分组；用量记录支持编辑/删除；App 后台自动暂停刷新。analyze 零问题、21 个测试通过，APK v0.2.2（49.9MB）已用正式签名打包。

影响：更新链路可用且安全（进度、校验、正式签名）；账目展示更准确；用量可修正。

备注：keystore 与密码需用户自行备份，丢失无法覆盖升级。

## 日期：2026-08-07

完成：手机 App 更新提示增强（v0.2.1）——更新弹窗显示 Release 更新说明与更新包大小（读 GitHub 资产 size），下载进度条显示"已下载 X / Y MB"。analyze 零问题、19 个测试通过，APK v0.2.1 已打包。

影响：用户更新前可知晓更新内容和所需空间。

备注：——

## 日期：2026-08-07

完成：手机 App 同步缓存命中字段与用量图表（每日柱状图金额/Token 切换 + Token 构成扇形图）；新增应用内更新 v0.2.0——启动自动检查 + 设置页手动检查 GitHub Release，下载 APK 并唤起系统安装器；修复 Kotlin 增量编译跨盘符报错（gradle.properties 关闭 kotlin.incremental）。analyze 零问题、17 个测试通过，APK v0.2.0（49.8MB）已打包。

影响：手机端与桌面/网页版图表功能对齐；后续发版只需 GitHub Release 传 APK，用户 App 内一键更新。

备注：更新源默认为 GitHub Releases API，可按需换自建地址。

## 日期：2026-08-07

完成：用量图表功能——桌面与网页版"按天消费金额 / Token 柱状图"（近 14/30 天）与"输入(命中缓存) / 输入(未命中缓存) / 输出"扇形图；用量表新增缓存命中拆分字段（旧库自动迁移）；CLI add-usage 支持 --cache-hit / --cache-miss。

影响：Token 用量从明细表升级为可视化统计，可直观看到每日消费、每日 Token 和输入输出构成；10 项测试通过。

备注：手机 App 已同步缓存命中字段与图表；图表演示记录（demo 账户）留在本地库中。

## 日期：2026-08-07

完成：手机 App APK 打包成功（app-release.apk 48.7MB，Android SDK 36.1.0 + 腾讯/阿里云构建镜像），待实机安装验证。

影响：手机端可分发安装测试。

备注：——

## 日期：2026-08-07

完成：手机 App 工程化——Flutter 3.44.8 安装（中国镜像）、生成 android/ios 平台工程、flutter analyze 零问题、7 个测试通过。

影响：手机 App 进入可构建状态。

备注：Android 工具链后续打包时补装。

## 日期：2026-08-06

完成：手机 App 项目创建（Flutter），复用桌面版查询逻辑（DeepSeek / OpenAI / 中转渠道 + Token 用量记录 + API Key 安全存储）。

影响：多端覆盖（桌面 / 网页 / 手机）。

备注：Flutter SDK 安装后构建验证。

## 日期：2026-08-06

完成：Windows 桌面应用（Tkinter）完成并通过真实数据验证；DeepSeek 真实余额验证通过；代码上传 GitHub。

影响：核心目标"实时获取余额"以桌面应用形式落地。

备注：——

## 日期：2026-08-05

完成：项目初始化。创建四件套文档，初始化 Git 仓库（main 分支），迁移至 D:\codexproject\模型余额。

影响：确立项目核心目标——实时获取模型 API 余额（多少钱）与 Token 用量。

备注：——
